---
title: Git
keywords: [git, version-control, gitconfig, github]
status: reviewed
---

# Git

Notes on configuring and tooling around Git itself — sane defaults, prettier diffs, profile pages.

## Articles

- [Git configuration — sane defaults](config.md) — copy-paste `~/.gitconfig` distilled from how Git's own core devs configure it.
- [delta — pretty diff & blame pager for Git](git_delta.md) — Rust pager that makes `git diff`/`git log`/`git blame` actually readable.
- [GitHub profile README](github.md) — turn the default GitHub user page into a personalised landing card.

## Keywords

`#git` `#version-control` `#gitconfig` `#github`
