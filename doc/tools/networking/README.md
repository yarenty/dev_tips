---
title: Networking
keywords: [networking, traceroute, mtr, diagnostics]
status: reviewed
---

# Networking

A small section so far — currently a single article on `mtr`. Most networking-adjacent tooling lives next door under [`tools/security/`](../security/README.md) (tunnels, VPNs, scanners, sniffers).

## Articles

- [[mtr]] — `traceroute + ping` in one continuous TUI display; the everyday "where is the latency coming from?" tool.

## Adjacent / cross-section

- [[rustscan]] — fast Rust port scanner (under `tools/security/`).
- [[sniffnet]] — friendly Rust GUI for live traffic monitoring (under `tools/security/`).
- [[ngrok]] — hosted HTTPS tunnel to localhost (under `tools/security/`).
- [[wireguard]] — modern in-kernel VPN (under `tools/security/`).
- [[trace]] — Trippy, a more modern TUI traceroute alternative (under `tools/linux/`).

## Keywords

`#networking` `#traceroute` `#mtr` `#diagnostics` `#tools`
